/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    'postcss-import': {},
    'postcss-simple-vars': {},
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
